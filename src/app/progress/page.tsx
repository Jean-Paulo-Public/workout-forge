
"use client";

import { AppLayout } from '@/components/layout/app-layout';

const ProgressTrackingPage = () => {
  return (
    <AppLayout>
      <div style={{ padding: '20px' }}>
        <h1>Acompanhamento de Progresso</h1>
        <p>Conteúdo mínimo da página de progresso.</p>
        <p>Se esta página renderizar sem erros de parsing, o problema está no código que foi removido.</p>
      </div>
    </AppLayout>
  );
};

export default ProgressTrackingPage;
